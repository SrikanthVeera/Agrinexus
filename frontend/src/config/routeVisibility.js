// src/config/routeVisibility.js

export const routeVisibility = {
  hideNavbarRoutes: ["/login", "/register"],
  showHeaderRoutes: [],
  showFooterRoutes: [
    "/",
    "/buyer/dashboard",
    "/farmer/dashboard",
    "/market-price",
    "/contact",
    "/about",
    "/service",
  ], // add only routes where you want Footer
};
