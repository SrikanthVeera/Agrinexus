import React from "react";

const AdminDashboard = () => (
  <div style={{ padding: 40, textAlign: "center" }}>
    <h2>Admin Dashboard (Minimal Test)</h2>
    <p>If you see this, your import and route are correct.</p>
  </div>
);

export default AdminDashboard;
